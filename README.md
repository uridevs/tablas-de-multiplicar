# Notas:

Programa simple creado en Node con fines didácticos.


```
Options:
      --help     Show help                                      [boolean]
      --version  Show version number                            [boolean]
  -b, --base     Es la base de la tabla de multiplicar          [number] [required]
  -l, --listar   Muestra la tabla en consola al ser creada
                                                                [boolean] [default: false]
  -h, --hasta    Hasta que número se debe crear la tabla        [number] [default: 10]
```